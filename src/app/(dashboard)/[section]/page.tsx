import { EmptyState, PageHeader } from "@/components/ui/page";
const labels: Readonly<Record<string, string>> = { patients: "Patients", samples: "Samples", tests: "Tests", hematology: "Hematology", biochemistry: "Biochemistry", "urine-analysis": "Urine Analysis", electrolytes: "Electrolytes", reports: "Reports", doctors: "Doctors", instruments: "Instruments", "quality-control": "Quality Control", users: "Users", suppliers: "Suppliers", appointments: "Appointments", billing: "Billing", inventory: "Inventory", profile: "Profile", settings: "Settings" };
export default async function SectionPage({ params }: Readonly<{ params: Promise<{ section: string }> }>) { const { section } = await params; const title = labels[section] ?? "Workspace"; return <><PageHeader title={title} description={`${title} foundation is ready for its Phase 2 workflow.`}/><EmptyState title={`${title} module`} detail="The shared architecture, routes, feedback states, forms, tables, and API boundary are in place."/></>; }
