import type { ReactNode } from "react";
export function PageHeader({ title, description, action }: Readonly<{ title: string; description: string; action?: ReactNode }>) { return <div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[0.15em] text-[#176b87]">Laboratory operations</p><h1 className="text-2xl font-bold tracking-tight sm:text-3xl">{title}</h1><p className="mt-2 text-sm text-[color:var(--muted)]">{description}</p></div>{action}</div>; }
export function StatusBadge({ children, tone = "neutral" }: Readonly<{ children: ReactNode; tone?: "success" | "warning" | "neutral" }>) { const styles = { success: "bg-emerald-50 text-emerald-700", warning: "bg-amber-50 text-amber-700", neutral: "bg-slate-100 text-slate-600" }; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${styles[tone]}`}>{children}</span>; }
export function EmptyState({ title, detail }: Readonly<{ title: string; detail: string }>) { return <div className="grid min-h-72 place-items-center border border-dashed bg-surface p-8 text-center"><div><p className="font-semibold">{title}</p><p className="mt-2 text-sm text-[color:var(--muted)]">{detail}</p></div></div>; }
