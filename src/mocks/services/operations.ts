import { apiClient } from "@/lib/api/client";
import type { Appointment, InventoryItem, Invoice } from "@/types/domain";
const delay = <T,>(item: T) => new Promise<T>((resolve) => setTimeout(() => resolve(item), 240));
const store = <T extends { id: string }>(seed: readonly T[]) => { let data = [...seed]; return { list: () => apiClient.request(() => delay([...data])), getById: (id: string) => apiClient.request(() => delay(data.find((row) => row.id === id) ?? null)), create: (input: Omit<T, "id">) => apiClient.request(() => { const row = { ...input, id: `${Date.now()}` } as T; data = [row, ...data]; return delay(row); }), update: (id: string, input: Partial<T>) => apiClient.request(() => { const current = data.find((row) => row.id === id); if (!current) throw new Error("Record not found"); const row = { ...current, ...input }; data = data.map((item) => item.id === id ? row : item); return delay(row); }), delete: (id: string) => apiClient.request(() => { data = data.filter((row) => row.id !== id); return delay(undefined); }) }; };
export const appointmentApi = store<Appointment>([{ id: "apt-01", patientId: "pat-01", doctorId: "doc-01", date: "2026-08-23", time: "10:30", type: "Consultation", status: "Upcoming", appointmentLink: "https://meet.example/apt-01", createdBy: "Dr. Ananya Rao" }]);
export const billingApi = store<Invoice>([{ id: "inv-01", billNumber: "INV-260822-018", patientId: "pat-01", doctorId: "doc-01", billDate: "2026-08-22", items: [{ description: "Complete Blood Count", quantity: 1, mrp: 450 }], discount: 20, sgst: 19.35, cgst: 19.35, total: 468.7, paymentStatus: "Paid", addedBy: "Dr. Ananya Rao" }]);
export const inventoryApi = store<InventoryItem>([{ id: "invty-01", medicine: "EDTA Vacutainer", stockQuantity: 18, purchasePrice: 11, salePrice: 18, stockHolder: "Central Store", batchNumber: "EDTA-2411", expiryDate: "2027-01-31", reorderLevel: 25 }, { id: "invty-02", medicine: "Glucose Reagent", stockQuantity: 52, purchasePrice: 280, salePrice: 390, stockHolder: "Biochemistry", batchNumber: "GLU-2408", expiryDate: "2026-11-30", reorderLevel: 20 }]);
